{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Compile",
            "type": "f5anything",
            "request": "launch",
            "command": "esphome compile ${workspaceFolderBasename}.yaml"
        }
    ]
}
